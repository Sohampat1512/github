git clone <URL>
git init
git config --global user.name "<USERNAME>"
git config --global user.email "<EMAIL>"

git remote -v --> to know the source
git add <filename>
git add .

git commit -m "First commit"
git push 
